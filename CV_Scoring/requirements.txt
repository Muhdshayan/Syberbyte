# SmartRecruit RAG AI Module - Requirements
# ⚠️ IMPORTANT: Install PyTorch with CUDA first (see setup instructions below)
# Core AI/ML Libraries
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
numpy>=1.21.0
sentence-transformers>=2.2.0

# GGUF Model Support with CUDA (for Mistral & CodeLlama)
# Install with: CMAKE_ARGS="-DLLAMA_CUBLAS=on" pip install llama-cpp-python --force-reinstall --upgrade --no-cache-dir
llama-cpp-python>=0.2.0

# ONNX Runtime for DistilBERT (GPU Support for RTX 3050)
onnxruntime-gpu>=1.15.0

# Alternative CPU-only ONNX (if GPU issues)
# onnxruntime>=1.15.0

# Data Processing
pandas>=1.5.0
scikit-learn>=1.1.0

# Text Processing & NLP
transformers>=4.21.0
tokenizers>=0.13.0
regex>=2022.7.9

# Utilities
tqdm>=4.64.0
requests>=2.28.0

# JSON/Data Handling
jsonschema>=4.0.0

# Logging & Configuration
pyyaml>=6.0
python-dotenv>=0.19.0

# Optional: For enhanced text processing
spacy>=3.4.0
nltk>=3.7

# Vector Database for RAG
chromadb>=0.4.0

# Optional: For better embeddings (if needed)
faiss-cpu>=1.7.0

# Development/Testing (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0